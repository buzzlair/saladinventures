/** @type {import('next').NextConfig} */
const nextConfig = {
  // No special output needed for Cloudflare Pages Next.js adapter
  // You can add custom config options here if needed
  // Example: reactStrictMode: true,
};

module.exports = nextConfig;
